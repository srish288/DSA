#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n;
    cin>>n;
    vector<int> s;
    for(int i=0;i<n;i++){
        cin>>s[i];
    }
    int travel=0;
    int i=0;
  for(int i=0;i<n;i++){
    int y=abs(travel-s[i]);

  }
   
    
    return 0;
}